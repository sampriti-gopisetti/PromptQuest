{
  "name": "prompt-quest",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix Frontend",
    "dev": "concurrently \"npm run start --prefix Frontend\" \"python app.py\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
